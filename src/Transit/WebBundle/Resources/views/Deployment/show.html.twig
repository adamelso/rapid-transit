{% extends "TransitWebBundle:Deployment:index.html.twig" %}

{% block content %}

  <h2>{{ deployment.id }}</h2>

  <h2>{{ deployment.branch }}</h2>

  <p>{{ deployment.project }}</p>

{% endblock content %}
